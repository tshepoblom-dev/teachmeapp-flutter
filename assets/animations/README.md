Drop the two onboarding Lottie JSON files here, named exactly:

- `start_learning.json` — used on the "Start learning now!" slide
- `teach_your_skills.json` — used on the "Teach your skills!" slide

See `lib/features/onboarding/presentation/screens/onboarding_screen.dart` for the `Lottie.asset(...)` references.
